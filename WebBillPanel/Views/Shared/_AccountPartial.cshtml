@if (Session["_idUser__"] != null)
{
    <li class="nav-item dropdown">
        @using (Html.BeginForm("Logout", "Panel", FormMethod.Post, new {id = "logoutForm"}))
        {
            @Html.AntiForgeryToken()
        }
            <a class="nav-link dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i>Cuenta</a>
            <div class="dropdown-menu dropdown-primary dd-right" aria-labelledby="dropdownMenu1" data-dropdown-in="fadeIn" data-dropdown-out="fadeOut">
                <a class="dropdown-item" href="@Url.Action("Index", "Home")">Página Principal</a>
                <a class="dropdown-item" href="javascript:document.getElementById('logoutForm').submit()">Cerrar Sesión</a>
            </div>
    </li>
}
else if (Session["_idVenta__"] != null)
{
    <li class="nav-item dropdown">
        @using (Html.BeginForm("LogoutDocument", "Panel", FormMethod.Post, new {id = "logoutForm"}))
        {
            @Html.AntiForgeryToken()
        }
            <a class="nav-link dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i>Cuenta</a>
            <div class="dropdown-menu dropdown-primary dd-right" aria-labelledby="dropdownMenu1" data-dropdown-in="fadeIn" data-dropdown-out="fadeOut">
                <a class="dropdown-item" href="@Url.Action("Index", "Home")">Página Principal</a>
                <a class="dropdown-item" href="javascript:document.getElementById('logoutForm').submit()">Cerrar Sesión</a>
            </div>
        
    </li>
}
else if (Session["_idAdmin__"] != null)
{
    <li class="nav-item dropdown">
        @using (Html.BeginForm("LogoutAdmin", "Home", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()
        }
        <a class="nav-link dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i>Cuenta</a>
        <div class="dropdown-menu dropdown-primary dd-right" aria-labelledby="dropdownMenu1" data-dropdown-in="fadeIn" data-dropdown-out="fadeOut">
            <a class="dropdown-item" href="@Url.Action("Index", "Home")">Página Principal</a>
            <a class="dropdown-item" href="javascript:document.getElementById('logoutForm').submit()">Cerrar Sesión</a>
        </div>

    </li>
}
else
{
    <li class="nav-item ">
        <a class="nav-link" href="mailto:admin@tisoluciones.net"><i class="fa fa-envelope"></i> <span class="hidden-sm-down">Contactar</span></a>
    </li>
}
